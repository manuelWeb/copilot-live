// importer sum, multiply, divide depuis le fichier lambda-fn/math.js et utilise les
import { sum, multiply, divide } from './lambda-fn/math.js';

console.log(sum(1, 2)); // 3
console.log(multiply(1, 2)); // 2
console.log(divide(1, 2)); // 0.5
